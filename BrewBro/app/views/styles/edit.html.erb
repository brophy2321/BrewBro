<h1> Edit Style </h1>
<%= form_for :style, url: style_path(@style), method: :patch do |f| %>

  <% if @style.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@style.errors.count, "error") %> prohibited
        this style from being saved:
      </h2>
      <ul>
        <% @style.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :style %><br>
    <%= f.text_field :style %>
  </p>
  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', styles_path %>
